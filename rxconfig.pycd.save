cd ~/noxuscmmd
sudo systemctl stop noxus-reflex.service
source .venv/bin/activate

# 1. Borrar toda la compilación frontend y cachés
rm -rf .web
rm -rf ~/.bun/install/cache
rm -rf ~/.local/share/reflex/bun

# 2. Eliminar paquetes frontend problemáticos del rxconfig.py
nano rxconfig.py
